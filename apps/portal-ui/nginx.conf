server {
  listen 80;
  server_name _;
  absolute_redirect off;

  root /usr/share/nginx/html;
  index index.html;

  location = / {
    return 302 /portal/;
  }

  location /portal/ {
    try_files $uri $uri/ /portal/index.html;
  }
}
